version: "3.9"

services:
  sentiment-app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ""
    container_name: sentiment-app
    ports:
      - "8000:8000"
    restart: unless-stopped